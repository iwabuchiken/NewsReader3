<%= form_for(@keyword) do |f| %>
  <% if @keyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keyword.errors.count, "error") %> prohibited this keyword from being saved:</h2>

      <ul>
      <% @keyword.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
    <%#= f.number_field :category_id %>
    <%# if Category.all != nil %>
    <% if Category.all.size > 0 %>
    	<%= select("keyword", "category_id", Category.all.collect {|g| [g.name, g.id]}) %>
	<% else %>
		<b>No data</b>
	<% end %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
